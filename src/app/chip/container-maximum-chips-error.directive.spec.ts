import { ContainerMaximumChipsErrorDirective } from './container-maximum-chips-error.directive';

describe('ContainerMaximumChipsErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerMaximumChipsErrorDirective();
    expect(directive).toBeTruthy();
  });
});
